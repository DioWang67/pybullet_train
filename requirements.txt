# Core RL stack
stable-baselines3[extra]==2.3.2
gymnasium==0.29.1

# Physics simulation
pybullet==3.2.6

# Numerical
numpy>=1.24,<2.0

# Visualization / logging (optional but used by SB3 callbacks)
tensorboard>=2.14
tqdm>=4.66
rich>=13.0

# Notebook utilities (Colab already has these, pinning for local dev)
ipykernel>=6.0
ipywidgets>=8.0
